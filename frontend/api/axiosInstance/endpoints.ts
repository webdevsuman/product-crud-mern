const productsURL = "/api/products";

export const endpoints = {
  product: {
    getAll: productsURL,
    create: productsURL,
    details: productsURL,
    update: productsURL,
    delete: productsURL,
  },
};
